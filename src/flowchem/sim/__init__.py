"""
flowchem.sim — simulation layer for FlowChem.

Start with `flowchem-sim <config.toml>` instead of `flowchem <config.toml>`.
Every real device class is transparently replaced by its simulated counterpart;
the TOML file requires no changes.
"""
